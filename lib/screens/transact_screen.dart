import 'package:flutter/material.dart';

class TransactScreen extends StatelessWidget {
  const TransactScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('TransactPage'),
    );
  }
}
